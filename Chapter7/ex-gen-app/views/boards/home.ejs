<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/stylesheet.css" />
  </head>
  <body class="container">
    <header>
      <h1><%= title %></h1>
    </header>
    <div role="main">
      <p class="h4"><%= userName %>'s messages.</p>
      <table class="table mt-5">
        <% for(let i in content) { %>
        <%- include("data_item", { val: content[i] }) %>
        <% } %>
      </table>

      <ul class="pagination justify-content-center">
        <li class="page-item">
          <a class="page-link" href="/boards/home/<%= userName %>/<%= accountId %>/<%= page - 1 %>">&lt:&lt; prev</a>
        </li>
        <li class="page-item">
          <a class="page-link" href="/boards/home/<%= userName %>/<%= accountId %>/<%= page + 1 %>">next &gt:&gt;</a>
        </li>
      </ul>
    </div>
    <div class="text-left">
      <a href="/boards" class="btn btn-outline-secondary">TOPへ</a>
      <a href="/users/logout" class="btn btn-outline-secondary">ログアウト</a>
    </div>
    
  </body>
</html>
